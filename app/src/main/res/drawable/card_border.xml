<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" /> <!-- Change this to your card background color if different -->
            <stroke android:width="18dp" android:color="#00008B" /> <!-- Navy blue color -->
        </shape>
    </item>
</layer-list>